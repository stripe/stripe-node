# Migration guide for v23

v23 of the SDK bumps the API version to `TKTK`. See the API changelog for more information: TKTK

## Use the V2 list response shape for V2 list methods

V2 list methods return `Stripe.V2List<T>`, whose pagination fields are `next_page_url` and `previous_page_url`. In previous versions, `Stripe.V2List<T>` also declared the V1-only `object`, `has_more`, and `url` fields for type compatibility, even though those fields were never populated at runtime.

There is no specific migration for this change. To paginate a V2 list response, you can continue to use the V2 pagination URLs:

```
const events: Stripe.V2List<Stripe.V2.Core.Event> = await stripe.v2.core.events.list();
if (events.next_page_url) {
  // ...
}
```

## Use `V2AddressParam` for V2 request addresses

V2 request address properties that include `town` now use `Stripe.V2AddressParam` instead of the Japan-specific `Stripe.JapanAddressParam`. Update explicitly typed V2 addresses to use the neutral V2 type:

```ts
// Before
const address: Stripe.JapanAddressParam = {
  line1: '123 Main Street',
  town: 'Brooklyn',
};

// After
const address: Stripe.V2AddressParam = {
  line1: '123 Main Street',
  town: 'Brooklyn',
};
```

V1 Kana and Kanji address properties continue to use `Stripe.JapanAddressParam`.

<!-- This is the migration guide for the next major version!
If you're making breaking changes, add a new h2 header with a nice title and write a detailed guide to help users upgrade.
You will almost certainly need before/after code examples and information about which users this change affects.
See: https://github.com/stripe/hark#writing-a-great-migration-guide
-->
