export default function Home() {
  return (
    <main>
      <h1>Stripe Webhook Example</h1>
      <p>This is a Next.js example for handling Stripe webhooks.</p>
      <p>Send POST requests to /api/webhooks to test the webhook handler.</p>
    </main>
  );
}
